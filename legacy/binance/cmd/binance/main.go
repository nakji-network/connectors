// TODO: handle the fact that binance websocket connections are limited at 24 hours.

package main

/*

var groupID = "binance-source"
*/
func main() {
	//conf := config.GetConfig()

	//kconn := kafkautils.New(conf.GetString("kafka.url"), conf.GetString("kafka.topic"), groupID)
	//binance.Init(kconn)
}
